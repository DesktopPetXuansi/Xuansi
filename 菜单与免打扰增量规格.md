# 菜单与免打扰增量

2026-09-13；直接上级为《实施规格.md》，依据用户本轮明确要求实施，不改变本地处理和手动启麦的约定。开始时间：14:30:41（Asia/Shanghai，终端记录）。

1. 软件和托盘提供日志、配置入口；日志读取后台完成，显示最近的有限行，支持过滤、跟随和刷新；不增加聊天、截图、录音正文日志。
2. 配置支持 temperature、top_p、回复 token 上限、上下文大小、CPU 线程、GPU 层数、图文模型路径，以及可修改的快捷键。校验后原子保存，旧设置自动补默认值；实际请求及引擎启动使用配置。
3. Windows 音频会话检测只读其他应用输出状态；开口前及播放中检测，发现其他应用发声就跳过或停止本次朗读，文字仍显示。静音结束后不补播旧回复；检测不可用时保留文字。轮询存在响应时间，不能承诺硬实时零重叠。
4. Ctrl+Alt+Space 默认打开右下角快捷对话框；再次按下或 Esc 隐藏。仅用户触发时获得输入焦点；与主面板共享当前对话、人设和记忆。冲突须可见，保存失败保留原设置。

任务顺序：音频回避与测试 → 参数配置与日志 → 全局热键与快捷对话 → 原生 UI / 实机检查 → 记录结果、更新安装依赖。

验收包括其他声音开口前阻止/中途打断/静音恢复/检测失败、自身声音排除、参数传递与越界、日志轮转与有界显示、热键冲突和注销、右下角位置及双窗口聊天同步。测试仅使用虚拟内容；本轮不改流式语音，不改用户长期记忆。
