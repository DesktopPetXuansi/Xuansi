# 模型控制朗读验收

日期：2026-09-25。直接上级：[实施规格](实施规格.md) 的“模型理解朗读开关”增量。依据用户明确要求由模型理解停止和恢复说话，并选择只关闭朗读、保留已开启的麦克风。

## 实现与范围

- 同一次本地模型生成先返回受 GBNF 约束的 `on/off/keep` 控制头，再流式生成正文。没有用户输入关键词匹配，也没有新增分类请求、依赖或云端服务。
- 完整合法控制头才能执行；跨包解析后先执行动作再向 TTS 交付正文。控制头不显示、不朗读、不写入对话历史；无效或截断协议报告失败。
- 关闭时阻止对话、自动观察和试听发声，连续收音状态保持；恢复当轮即创建流式朗读，观察仍服从原偏好和声音回避。静音状态下普通聊天继续保留文字和上下文。
- 状态作用于本次运行，不另存配置。重启或保存表单设置后恢复已保存偏好；仅换形象或切换声音回避不重置它。两个对话窗口同步显示朗读状态，未保存的人设草稿不受影响。
- 自动观察不提供控制回调；过期请求无法改变朗读状态。现有默认不自动启麦、全屏/休眠停止对话和本机处理约定保留。

## 验证依据

源码与官方文档核对：本机 Python 3.13.15、PySide6 Essentials 6.11.2、llama.cpp build 10930（56381e407）。参考当前 [GBNF 官方说明](https://github.com/ggml-org/llama.cpp/blob/master/grammars/README.md)、[服务接口文档](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/README.md) 和 [Qt Signal 文档](https://doc.qt.io/qtforpython-6/PySide6/QtCore/Signal.html)。本地实测确认当前引擎兼容 `grammar` 与流式响应组合。

先记录旧实现测试基线 92 项通过，再增加控制头、HTTP 和运行时失败测试，确认缺少接口时失败后实施。最终 `python -X utf8 -m pytest -q --tb=short`：111 项通过（29.66 秒）；`ruff check pet tests scripts`、`compileall` 及 `git diff --check` 通过。

真实模型脚本：`python scripts/verify_model_speech.py`。2026-09-25 00:19 运行 Qwen3.5-4B-Q4_K_M 和真实 Melo TTS，使用合成输入、临时记忆及静音播放接收器。报告保存在本机忽略目录 `data/verification/model-speech-report.json`。

| 类别 | 结果 |
| --- | --- |
| 直接关闭、开会只用文字、旁人睡觉时打字 | 3/3 关闭 |
| 恢复出声、别停我还在听、不要关闭语音 | 3/3 开启 |
| 两种翻译引用、谈论他人、假设问题、询问设置方法 | 5/5 保持 |
| 静音中的普通追问 | 1/1 保持 |
| 结合前文理解“现在可以了” | 1/1 恢复 |
| 连续三轮真实语音链路 | 关闭 0 音频片段；普通回复仍为 0；恢复当轮 2 段，44100Hz |

语义样例固定 temperature=0；三轮运行时使用默认 temperature=0.7。模型预热后的首段可见文字约 0.186–0.464 秒；冷启动首段约 3.621 秒。恢复那轮总计约 5.652 秒，包含首次加载 Melo 和生成两段音频，不能当成预热后的响应耗时。

初次真实样例发现翻译引文被误判为关闭，第二次发现询问设置方法被误判；通过在模型提示中说明任务边界、否定关系及回复与动作一致性修正，没有加入关键词兜底。最终 13 个样例全部通过；这不代表任意说法或任意替换模型都不会误判。

原生 UI 脚本：`QT_QPA_PLATFORM=offscreen python scripts/verify_speech_ui.py`（PowerShell 使用 `$env:QT_QPA_PLATFORM = 'offscreen'` 设置）。验证真实 Qt 输入事件、主面板关闭与快捷窗口恢复、两处状态和文本同步、麦克风保持、人设草稿保留、相同设置重新保存也恢复默认。截图 `data/verification/speech-muted-panel.png`、`speech-muted-quick.png` 已目视检查，状态和按钮可读、无裁切。仅截图测试窗口，不读取用户工作桌面。

现有声音回避验收全部通过，包含模拟保存失败时回滚。既有菜单脚本在 offscreen 模式下 Esc 快捷键项目未通过；切回原生 Windows 窗口后，Esc、热键、日志、配置保存及双窗口对话各项均通过，未为此修改快捷键逻辑。UI 验收清理已等待 `shutdown_done`，退出无遗留任务；真实模型脚本也已关闭其启动的引擎。

## 边界

- 当前仍为半双工：朗读期间暂停识别，口头指令需等本轮朗读结束；文字新请求可以中断上一轮。此次未实现回声消除或语音插话。
- 不读取或修改用户现有记忆、偏好；真实验证未打开物理麦克风或扬声器，用户的实际收音与主观听感尚未人工验收。
- 本次改动为本地源码，没有重新打包或发布安装包。

## 实际投入与完成状态

从本轮可核验的 00:01:58 源码检查时间到 00:25 左右的最终验收，协作经过时间约 25 分钟，按 5 分钟取整；包含本地测试运行和短暂交互等待，不冒充精确主动工时，也不计用户线下投入。更早的初始检查未取得单独时间戳，不补造起始时间。

代码、使用说明、自动化和真实模型样例验收已完成；上述物理麦克风、扬声器人工体验及安装包发布不在此次已完成验收范围内。
