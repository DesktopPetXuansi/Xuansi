# 2026-09-14 GitHub 源代码发布验收

直接上级：[实施规格](实施规格.md) 的“GitHub 源代码发布”增量，以及用户将现有代码推送至 `DesktopPetXuansi/Xuansi` 的明确请求。用户确认保留远端现有 Apache-2.0，并明确“图片保留版权，代码按 Apache-2.0 开源”。

## 时间与当前状态

- 可验证开始时间：2026-09-14 11:39:09（北京时间，首次检查命令时间）。
- 发布提交时间：2026-09-14 16:34:38（北京时间，Git 提交时间）；随后 GitHub API 核验成功，16:36:20 的时钟检查已完成核验。
- 当前等级：A，本轮源代码发布、文档及远端验收完成；不代表商业安装包或主观体验验收完成。
- 实际投入：约 25 分钟主动投入估算，依据 11:39–12:02 的准备检查及 16:34–16:36 的提交核验时段，精确到 5 分钟；未把两段之间无法核实的长间隔计入。工具执行及异步问答可能包含短暂等待，没有计入不可观察的用户线下投入。

时间核对说明（2026-09-14）：补记草稿曾按连续工作假设填写“12:04”，提交前查询 Git 实际时间发现不符，现以上述可验证时间更正，不将长间隔伪计为工作投入。

## 交付内容

- 重写公开 README：功能、Windows 安装、固定模型目录限制、菜单用法、语音非流式、记忆、整屏观察、透明动图、数据位置、测试和许可范围。
- 保留远端 Apache-2.0 `LICENSE` 原文，新增项目 `NOTICE`；玄司立绘/头像新增单独保留版权声明，历史 NekoAI MIT 原文保留。
- 更新 `THIRD_PARTY.md`，区分项目代码、模型、Qt、语音资源和 NVIDIA 运行库许可。明确当前不属于已完成许可审计的商业安装包。
- 增补凭据及模型忽略规则；模型、运行环境、设置、记忆、日志、录音、截图和导入形象副本不随 Git 发布。
- 将远端初始历史合入本地，不覆盖已有提交，不使用强制推送。

## 已获得的证据

| 验收项 | 实际结果 |
| --- | --- |
| 目标仓库 | GitHub API 核对 `DesktopPetXuansi/Xuansi`，公开仓库，默认分支 main，当前登录具有推送权限 |
| 远端原始提交 | `f62eb74fc055a31ba7f5e47088065a328a29162f`，当时只含 LICENSE |
| 历史合并 | `e7d5eb5` 合并远端初始历史，已有本地功能提交保留 |
| LICENSE 未更改 | 本地与原远端 LICENSE 的 Git blob 均为 `261eeb9e9f8b2b4b0d119366dda99c6fd7d35c64` |
| 历史文件检查 | 扫描 208 个 blob，最大文件 2,531,454 字节；未发现检查模式覆盖的密钥、令牌、用户目录路径或运行数据；结果在本机忽略的 `data/verification/publication-audit.json` |
| 自动测试 | 2026-09-14 11:59 执行 `python -m pytest -q`，62 passed in 3.20s |
| 静态检查 | 同轮 `python -m ruff check pet tests scripts` 通过；`git diff --check` 无空白错误 |
| 人工文档抽查 | 对照 `setup.ps1`、下载脚本、配置和应用入口，核对 Python 版本、两处 MODELS 常量、全量模型下载、单实例及 smoke 行为；对照上游许可原文写明独立许可范围 |
| 发布文件及链接 | 待提交树共 150 个文件，检查 34 个本地文档/图片链接全部存在；未发现检查模式覆盖的敏感内容或运行数据，记录于本机 `data/verification/publication-tree-audit.json` |
| 实际推送 | `git push -u origin main` 成功，远端由 `f62eb74` 正常推进至 `45ff99835e557ad4496f40732393747efecf1d37`，未强制推送 |
| GitHub 远端核验 | API 返回 main 提交与本地一致，README、NOTICE、源码及素材声明可见；许可证接口返回 Apache-2.0，LICENSE blob 与原值相同；推送后本地工作区干净 |

本轮只调整文档、声明和忽略规则，不修改运行逻辑、不重新下载模型、不重启用户桌宠。已有原生界面与真实模型验收记录继续有效，但不把之前的测试说成这次重新执行。

## 剩余动作与边界

本轮约定的源代码发布动作已完成。本记录在发布核验后补记结果，随文档后续提交同步；不把推送前检查冒充远端结果。

商业安装包的逐组件分发审计、SenseVoice 目标商用授权确认、全新硬件适配和用户主观体验验收不在本轮发布范围，尚未完成。没有发布二进制 Release、模型权重或本机数据。

用户随后新增“把语音改为流式”，并选择“回复边生成边朗读，先降低回答等待时间”。这是发布后的独立功能增量，先核对 SSE 和语音输出接口，再做取消与声音回避测试；本记录只验收发布，不把尚未完成的流式语音记为已交付。未来如开始商业发行，仍需先确定识别模型许可和分发方案。
