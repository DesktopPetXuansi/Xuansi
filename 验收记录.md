# 本地 AI 桌宠验收记录

本记录属于 `实施规格.md`，直接上级是本次用户授权的桌宠任务。修改范围仅为独立的 `desktop-ai-pet` 项目、其 D 盘模型目录与启动快捷方式。

## 交付状态

**B：核心应用已实现、安装并启动，自动检查和虚拟场景通过；真实连续语音已有运行证据，识别准确性、听感及日常工作流畅度仍待用户确认。** 不写成用户已验收，也不把合成音频测试当作真实设备验收。

桌面和项目内均已建立快捷方式。2026-09-13 09:39 首次启动；11:10 该实例已退出。14:09 启动最终代码，麦克风默认关闭，实际局部图文推理已恢复正常。无开机启动项，无云端服务配置，无对外发布。

## 已验证的能力与证据

| 能力 | 结果 | 本机证据 |
| --- | --- | --- |
| 真实模型、视觉输入 | “开始整理”按钮、待处理文件数 3 与人工基准一致；更换人设后回答名字“团子” | `data/verification/inference-report.json`、`vision-fixture.png` |
| 模型取消与回收 | 取消请求后自有模型进程结束；Windows Job Object 关闭后回收测试子进程 | 推理报告、`tests/test_process_guard.py` |
| 连续语音流程 | 已知合成语音 → SenseVoice → LLM → 本地 TTS → 测试播放接收器成功 | `data/verification/conversation-report.json` |
| 真实收音与回复运行 | 09:58—10:01 开麦后连续 5 次完成识别、文本推理和语音合成；未见对应错误记录，休眠后麦克风与模型退出 | `data/pet.log`；不包含语音正文 |
| 默认语音清晰度基准 | “你好，我是糯米。我会在这里陪着你。”回听识别正文完整；44100Hz | `data/verification/audio-report.json`、`voice-fast.wav` |
| 长期记忆 | 识别并保存“我喜欢喝绿茶”；释放模型、丢弃短期上下文、重读文件后仍能回答“绿茶” | `conversation-report.json`、记忆回归测试 |
| 忘记与快速开关 | 清空记忆取消旧回复并清空上下文；旧麦克风代次不覆盖新代次；语音错误不被“已关闭”覆盖 | `tests/test_runtime.py`、`tests/test_audio.py` |
| 鼠标 → 局部画面 → 气泡 | 在虚拟窗口注入点击事件，真实截图识别“开始整理”；气泡显示前后前景 HWND 不变 | `data/verification/observation-report.json` |
| 原生界面 | 已人工查看聊天/偏好/记忆等自身界面图片；透明区穿透、实体猫可命中、气泡保持焦点；全屏几何判断及隐藏/恢复逻辑通过 | `data/verification/desktop-report.json`、`panel-*.png` |
| 启动入口 | 无控制台启动与第二次启动返回 0；只产生一次应用启动，已有面板通知成功，退出后锁释放 | `data/verification/launch-report.json` |
| 代码检查 | 23 项 pytest 通过；Ruff 检查与 compileall 通过 | `tests/`、本次终端结果 |

桌面窗口复测曾因 Windows 不允许后台测试窗取得前景而无法直接模拟全屏。最终脚本读取虚拟窗口的真实全屏几何并送入暂停逻辑；这部分是状态注入验证。首次前景测试中全屏检测、隐藏和恢复实际通过，最新鼠标观察中显示气泡前后的焦点检查也通过。`fixture_has_focus=false` 保留为测试环境限制，不把它伪装成通过的前景激活。

鼠标观察验收为了获得可检查的答案，额外要求模型回答按钮名称；产品默认仍允许“无需回应”。测试不真实移动用户鼠标或点击用户应用；事件分类和过期判断另有逻辑测试。混合 DPI 多显示器的完整人工体验尚未覆盖。

## 本机资源测量

环境：Windows 11、i9-13980HX、RTX 4070 Laptop 8GB、约 32GB 内存。以下是本次测量，不保证其他负载或后续模型同样表现。

| 场景 | 测量结果 |
| --- | --- |
| 界面待机，关闭自动观察和麦克风 | RSS 约 251MB；5 秒样本 CPU 约 0.62%，按单个逻辑核的 100% 为口径 |
| 图文模型 | 采样峰值 RSS 约 3491MB；加载与推理期间平均约 94.89% 单核口径，运行优先级 BELOW_NORMAL |
| 整机显存 | 加载前 1848MB；图文后 5678MB；停止后 1835MB，差值约 3.8GB；包含其他应用，不能作为精确独占量 |
| 首次图文 | 含模型加载约 7.82 秒 |
| 已加载后的图文 | 重复同一测试图片约 0.66 秒，可能复用缓存，不能代表所有新画面 |
| 已加载后的简短文字 | 约 0.56 秒 |
| Melo 合成 2.75 秒语音 | 首次含加载 8.22 秒；已加载后 1.55 秒，CPU 4 线程 |
| SenseVoice 对该样例识别 | 首次含加载 3.39 秒；已加载后 0.24 秒，CPU 2 线程 |

原始测量见 `resources-report.json`、`audio-report.json`、`desktop-report.json`。默认自动观察开启时会间歇触发模型，上面的纯待机值不是推理期间的占用。实际运行会消耗算力，不承诺“完全零影响”。

14:10 补充实际运行证据：09:58—10:01 的 5 次收音长度为 1.5—5.4 秒，识别耗时约 0.14—0.41 秒，文字推理约 0.81—1.67 秒；对应较长回复的合成耗时约 2.66—6.26 秒，首次还另有模型加载等待。10:01:50 进入休眠后，麦克风关闭日志相差约 0.17 秒，模型进程结束相差约 0.62 秒。这只能证明本机链路运行，不能证明每个词都识别正确或声音主观满意。

14:09 最终启动后，真实局部图文首次约 4.10 秒，随后几次约 0.71—0.74 秒；当前屏幕已恢复可读取。保留此前 BitBlt 失败事实，不改写成从未发生。

## 数据与连接核验

模型和引擎文件通过 SHA256 校验；图文模型固定到仓库提交。Melo 包摘要由本次首次 HTTPS 下载后计算锁定，并非作者签名。校验文件与来源保存在 `assets-manifest.json`，许可证来源见 `THIRD_PARTY.md`。

推理服务只监听 `127.0.0.1`，未带凭据请求模型接口返回 401。模型运行时进程连接采样中，非回环连接为 0；Qt/asyncio 的本机回环连接属于正常内部连接。没有为了验收关闭用户整机网络，因此不把本次测试称为物理断网测试。

验收只保存虚拟画面、合成音频和测试资料。真实截图和录音在应用中不落盘；长期记忆为用户明确指定的本机 JSON。代码不执行模型输出，不记录对话、截图或窗口标题到运行日志。

## 保留的失败与修复

- 初始红圈遮住按钮文字，模型把“整理”读成“管理”；保留 `inference-occluded-baseline.json` 和对应图片。无遮字复测通过，但被遮小字仍属于模型边界。
- AISHELL3 虽能很快生成音频，多种音色对“糯米”等内容的回听识别仍有错字，因此撤出默认方案；记录在 `audio-aishell3-baseline.json` 和 `voice-candidates.json`。
- Kokoro 的本机延迟较高，保留为用户可选；Melo 在清晰度与延迟之间成为当前默认。
- 初次重复启动测试在窗口服务尚未就绪时连接失败；就绪后的复测通过，并增加最多约 8 秒的启动等待。
- Windows 前景限制曾让虚拟全屏测试未触发；保留 `desktop-focus-limited-baseline.json`，区分窗口几何、暂停状态和焦点验证。
- 修复快速语音开关竞态、清空记忆后仍可能引用旧上下文、过期观察可能朗读、超长上下文报错以及 Markdown 加粗正文被误删导致静音。
- 09:39 启动后遇到系统 BitBlt 截图失败，09:46 复现时前景 HWND 为 0。已增加无可用前景桌面时暂停观察、停止语音和释放模型的处理，桌面恢复后继续；不猜测这是锁屏、远程断开还是显示状态变化。静默回应后的“思考中”提示也已修正。

## 未完成的人工项与下一步

已向用户发出实际体验问题。随后从运行日志观察到连续语音和休眠成功，但尚未收到用户对识别准确性、听感与其他软件流畅度的文字确认，因此保持 B。无需重复执行已经观察到的合成闭环，只需针对实际使用反馈修正问题。

下次第一个动作是处理真实设备反馈：无法收音优先核对麦克风选择和 Windows 权限；无声音检查试听及输出设备；卡顿先关闭自动观察/休眠，再根据同一工作负载复测。真实混合 DPI 多显示器和高负载游戏体验也尚未人工验证。

## 实际投入

分段记录（2026-09-13 补充）：早段约 50 分钟协作经过时间，依据 08:55 首个核对命令与 09:43 首次验收记录；其后另有屏幕异常核查。继续收尾的可验证起点为 14:09，按最终文件/命令时间另计约 5 分钟。两段约 55 分钟属于有证据的协作经过时间估算，含短工具等待，不能等同于精确主动工时。中间长间隔、用户自行使用与后台常驻时间不计入 Codex 主动投入；未把 08:55—14:09 连续计为工作时长。

前一日首轮实施约 35 分钟协作经过时间（9 月 12 日约 21:16—21:50 的既有命令和文件记录，含工具等待）。本次与前一日分段记录，不串算为连续工作。用户人工/线下投入未提供，不作推测。核心应用可用，剩余人工体验未完成，等级保持 B。
