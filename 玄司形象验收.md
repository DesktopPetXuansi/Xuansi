# 玄司形象验收

2026-09-13；直接上级为《实施规格.md》的玄司形象调整。依据用户角色参考图、“名字为玄司”及“允许，使用本机脚本抠图”，完成本机素材与界面替换。

## 交付与验证

- `assets/xuansi/front.png` 为 1024×1536 RGBA，alpha 极值 0/255；头像 PNG/ICO 已生成。原始参考图没有覆盖。制作过程及提示词要点见 `assets/xuansi/README.md`。
- 桌宠使用该透明素材及 48 个缓存帧；提供待机呼吸、思考摆动、回应轻跳、休眠倾斜和移动动作。默认高度 160，可选 64、96、128、160、224、288。窗口、托盘、快捷对话、日志标题按名字显示。
- `pytest` 46 项通过，Ruff、compileall 和 `git diff --check` 通过。
- `scripts/verify_xuansi.py` 在真实 Windows Qt 窗口验证：六种尺寸、六种状态的所有帧均不为空且未接触画布边缘；同尺寸设置复用缓存；窗口和气泡显示不改变前景焦点；实体区域接收点击，透明角落穿透；点击触发面板信号，拖动只移动；面板、快捷对话、日志标题均包含“玄司”。证据：`data/verification/xuansi-report.json`。
- 已人工查看 `xuansi-preview.jpg` 的浅/深背景六种状态，以及 `xuansi-panel.png` 的自身窗口截图：角色全身完整，棋盘底色已移除，名称显示正确。测试未读取屏幕内容、用户记忆或麦克风。
- 默认尺寸帧像素缓存约 3.52 MiB（未含 Qt 对象/区域开销）；实际应用首轮缓存生成 0.035 秒。独立形象窗口三秒采样的单核 CPU 计时读数为 0.0%，仅表示短采样下低于该计时分辨率，不能解读为应用零资源消耗或模型推理占用。
- 创建桌面“玄司 AI 桌宠.lnk”，验证启动目标与新 ICO 路径正确；仅在旧“糯米”入口指向本项目且参数匹配时移除它。本目录原启动入口继续可用。
- 19:52 关闭旧解释器 PID 48744，确认无遗留子进程，再启动新版：启动器 PID 61208、解释器 PID 63404。19:52:54 日志记录玄司素材加载、声音回避、快捷键注册及“麦克风默认关闭”。原生窗口检查确认 120×160 角色窗口可见、进程优先级为 BelowNormal，证据 `xuansi-running.json`。
- 19:52:57 日志记录整屏捕获 `scope=screen size=1600x900`；19:53:00 本地图文推理完成，记录耗时 3.62 秒。正常运行仍仅在本机处理，不保存实际画面或回复正文作为本轮证据。

## 实现依据与边界

查阅当前 [Qt QPixmap 文档](https://doc.qt.io/qtforpython-6/PySide6/QtGui/QPixmap.html) 关于 alpha 蒙版的动态提取开销，以及 [QWidget 文档](https://doc.qt.io/qtforpython-6/PySide6/QtWidgets/QWidget.html) 的窗口区域与鼠标事件说明。缩放、变换及区域预先生成，动画定时器只切换缓存；隐藏时不刷新。

本轮形象是参考图经图像工具重绘后本机抠图的透明立绘，不能称为逐像素原图裁切。具有轻微身体动作，未制作眨眼、嘴型、侧后独立视图或 Live2D 骨骼。细发丝边缘存在少量灰色过渡，在深色背景已人工抽查；形象还原度仍待用户主观确认。

模型、声音回避、手动连续对话、记忆及全屏暂停沿用前轮实现。本轮没有重新进行真实麦克风/扬声器听感验收，也没有将语音改成流式。重启前确认无已保存设置文件，采用新默认称呼及桌面伙伴人设，未覆盖自定义配置或长期记忆。角色素材未指定公开再分发许可，没有向 GitHub 发布。

## 实际投入与状态

本轮可验证开始时间为 19:30:18；19:50 完成素材与原生窗口检查，19:52–19:53 完成启动入口和运行核验，19:54 整理验收。实际投入记为约 25 分钟协作经过时间，按 5 分钟取整，依据首轮命令、素材文件、测试及启动日志时间戳估算；未分段计时，可能包含两次图像生成和短确认等待，不冒充精确主动工时，不计作用户线下人工投入。

状态 B：核心替换、检查及本机启用已完成，已提交待用户确认外观；没有收到“用户验收通过”的回复。剩余为用户对还原度和实际桌面大小的主观体验反馈。下一动作是直接查看右下角玄司，必要时在偏好调整大小，无需重新下载模型或再次重启。
