# 整屏观察验收

2026-09-13；直接上级为《实施规格.md》。用户明确要求整屏并确认继续，因此默认范围改为鼠标所在显示器的完整画面；设置中保留“鼠标附近”，不合并其他显示器。所有画面仍只在本机内存中处理，模型下载与人设/记忆不变。

## 已验证

- 46 项 pytest 通过；Ruff 与 compileall 通过。新测试验证四角完整保留、1600 长边限制、纵横比、负坐标显示器、竖屏、鼠标红圈缩放、无对应显示器时停止抓图、范围持久化以及后台捕获参数传递。
- 人工 2560×1440 图片经过实际捕获编码路径，输出 1600×900。图文模型准确读出左上角“文件总数：3”和右下角“状态：待开始”，两项均远离鼠标位置，证明本次输入包含完整画面。证据：`data/verification/full-screen-report.json`、`full-screen-fixture.jpg`。
- 上述人工样例捕获编码耗时 0.077 秒（像素源为内存替身，不包含真实 Windows 抓屏时间）；图文推理含加载 6.59 秒。不能作为所有桌面内容的固定延迟保证。
- 已人工查看整屏样例和偏好页自身窗口截图。默认选项为“整块屏幕 · 鼠标所在显示器”，画面范围说明完整、可切换；证据：`full-screen-preferences.png`。
- 历史 `verify_observation.py` 只覆盖一个局部虚拟窗口，已明确设置 `capture_scope="nearby"`，防止测试因默认范围改变而采集真实桌面。新整屏验证使用人工像素源，没有保存实际桌面截图。

## 实现依据与边界

已查阅当前安装的 mss 10.2.0 源码、[mss 上游源码](https://github.com/BoboTiG/python-mss/blob/main/src/mss/base.py) 和 [Pillow thumbnail 文档](https://pillow.readthedocs.io/en/stable/reference/Image.html#PIL.Image.Image.thumbnail)。选取单显示器物理范围后等比例缩放，红圈在缩放后绘制。

整屏模式保留画面范围，不保证缩小后每个小字都能识别；被遮挡、最小化的窗口不会出现。原有全屏游戏/锁屏暂停、20 秒默认观察间隔、手动启麦及声音回避策略保留。实际工作画面的识别效果仍需使用体验，不写成用户已经验收。

## 投入与状态

首次只读检查时间 15:32:06；用户继续后，首个实现文件创建于 19:10:55，19:14:44 完成测试与人工图像检查，19:16:44 完成运行确认。本轮约 10 分钟主动投入估算，按 5 分钟取整，依据前期阅读命令及本轮实现/验收时间戳；短命令等待可能包含在内。15:34 至 19:10 的非主动工作间隔不计入，不计作用户线下工时。

状态 B：功能实现和必要检查已完成，已启动并确认实际整屏捕获；真实工作场景的识别体验仍待用户反馈。19:16 启动器 PID 57940、解释器 PID 48744，快捷键注册成功、麦克风默认关闭。19:16:33 的运行日志明确记录 `scope=screen size=1600x900 elapsed=0.338s`，19:16:37 完成本地图文推理，本次推理记录为 3.91 秒。运行日志仅记录范围/尺寸/耗时，没有保存实际画面或回复正文。

下一动作可直接使用自动观察，或打开快捷对话并勾选“附上屏幕画面”；不需要再次重启或下载模型。
