[tool.ruff]
target-version = "py313"
line-length = 110
exclude = ["upstream-nekoai", "runtime", ".venv", "data"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.lint.per-file-ignores]
"scripts/*.py" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]
