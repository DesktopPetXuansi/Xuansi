# 实时对话增量规格（2026-09-15 历史草案，已替代）

直接上级：`实施规格.md`。2026-09-15 按用户“我说话的时候 AI 也能立即回话”继续实施。
此前仅完成只读调研；本次可验证开始时间为 2026-09-15 18:04:58，跨日中断不计工时。

范围：手动开启后，实时中英识别、说话期间提前回应、朗读同时保持收音、扬声器回声消除。
保留本地处理、声音回避开关、模型可换、明确指令长期记忆。增加“边听边回”偏好，可切回原停顿识别。

实施顺序：
1. 固定并下载 Paraformer int8；验证在线解码和 WebRTC AEC3。
2. 同一双工声卡流提供实际播放参考，后台进行 AEC 和识别，声卡回调不运行模型。
3. 稳定文字达到阈值后每个话段提前回复一次，句末补充完整内容；播放期间继续识别。
4. 主面板和快捷窗口显示临时识别文字；关闭、换设备、休眠取消旧会话。
5. 合成音频验证连续输入、首个响应时间、回声与双讲；运行回归测试并记录实际限制。

验收：在输入音频结束前产生识别与回复音频；实际 AEC 对纯回声衰减且保留同时说话；
不将未定稿识别写入长期记忆；重复部分结果不重复触发；取消后无迟到声音。
声音回避开启时仍避让其他软件，关闭时允许同播。设备不支持双工时明确报错，可切回停顿模式。

边界：不是零延迟，也不保证不完整话语足以回答；首次模型加载、不同声卡和房间回声会影响体验。
只用虚拟/合成音频做自动验证，真实房间中的主观体验待用户试用。权重许可单独说明，不扩大代码许可。

## 2026-09-20 当前生效范围

修改依据：用户澄清“我话说完了之后，模型可以立刻回话”。之前的提前回答是理解偏差，未发布。
当前上级仍为 `实施规格.md`，仅本桌宠语音路径受影响，不修改接单计划。

采用：说话期间在线识别，只预览部分文字；停顿约 0.4 秒后提交最终话语，模型生成和分段朗读。
启麦时预热图文引擎与 TTS；朗读时沿用暂停收音，播完恢复，关闭可中止。
删除未发布的提前应答、双向声卡和 AEC 原型，不把旧草案的双讲指标当成本轮验收标准。
语音偏好新增默认开启的“实时识别（说完后尽快回复）”，关闭回退 SenseVoice 整句模式。
临时识别不进入记忆；最终文本沿用明确指令保存策略。长句持续说话时不因时长限制提前回答。

验证顺序：结束判断/句中不响应/关闭竞态测试 → 原生控件保存与显示 → 合成输入连接真实本地模型测量
“结束到首段音频”时间 → 更新文档、启动本机并提交推送。首段目标以本机预热后的短句实测为准，
不能承诺零延迟；真实设备缓冲、长停顿、噪声、较长的首段回复和慢音色仍会增加等待。
本轮开始时间：2026-09-20 16:38:39。验收与实际投入见 `实时语音验收.md`，跨日中断不计入。
