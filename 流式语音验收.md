# 2026-09-14 回复流式朗读验收

直接上级：[实施规格](实施规格.md) 的“回复流式朗读增量”。用户在授权 GitHub 发布后新增“把语音改为流式”，并选择“回复边生成边朗读，先降低回答等待时间”。原有手动启麦、自动断句、本地模型和声音回避继续适用。

## 当前状态与投入

- 可验证工作起点：2026-09-14 16:36:20（北京时间，接口核对时的时钟记录）。此前完成的 GitHub 发布另见 [开源发布验收](开源发布验收.md)。
- 当前检查时间：2026-09-14 16:50:27。
- 当前等级：B，逻辑和真实模型流水线验证通过，剩余最终回归、启动及 GitHub 同步。
- 截至当前实际投入：约 15 分钟主动投入估算，按 16:36–16:50 的连续工具与代码工作时间，精确到 5 分钟，可能包含短暂命令等待；不计入此前无法核实的长间隔和用户线下时间。

## 实现及接口依据

- `LocalEngine.chat` 增加可选增量回调，启用 llama.cpp `stream: true`，HTTPX 异步读取 SSE。其余调用沿用完整回复接口。
- 解析跨 UTF-8 字节边界的数据，忽略非正文和推理字段，屏蔽跨包 `<think>` 标签；异常或中断不当作完整成功回答，取消及断流清理本进程拥有的引擎。
- 普通语音、文字及附图对话在启用朗读时按正文分段合成。首段允许在至少两个字符后的逗号输出，后续软标点至少十二字符，单段最多四十八字符；长句优先在词间分段，小数点不作句末。
- 单合成任务、顺序播放，文本队列最多八段、已合成待播队列一段。后台预合成后续音频，限制积压；每段仍由现有 Melo/Kokoro 生成完整波形。
- 全轮每 50ms 检查代次和声音回避；播放阶段也检查。其他应用有声音或检测失效后锁存本轮静音，不因恢复安静而补播；取消先停声卡，再等待已进入 ONNX 的短段退出并丢弃结果。
- 完整文本保留在会话并送往现有面板/气泡；TTS 失败只降级文字。自动观察先判断静默标记和画面时效。识别仍按整句进行，不增加实时转写或全双工。
- 对话处理提取到 `pet/conversation.py`，运行时保留原有公开调度、配置、记忆和取消接口，避免主控制模块继续膨胀。

接口核对依据：[llama.cpp b10930 服务源码文档](https://github.com/ggml-org/llama.cpp/blob/b10930/tools/server/README.md)、[HTTPX 异步流式响应](https://www.python-httpx.org/async/)，以及已安装的 sounddevice、项目语音输出和 sherpa-onnx 调用源码。本轮没有新增模型、依赖或云端服务。

## 可重复的验证证据

单元/集成检查覆盖：首段在尾包前交付、UTF-8 切包、推理标签隐藏、断流和服务错误、取消关闭连接、早播与预合成、文本不遗漏、外部声音整轮抑制、合成期间取消无迟到播放、TTS 失败保留完整文字，以及原有声音回避、记忆和菜单逻辑。

真实测试命令为 `.venv/Scripts/python.exe -X utf8 scripts/verify_streaming.py`。使用当前 Qwen3.5 GGUF 和 Melo ONNX，临时记忆文件、人工构造的问候与桌面整理提示，不启麦、不截图；生成实际波形后交给静音测试接收器，未替代用户听感验收。

2026-09-14 16:50 的通过结果：

| 场景 | 首段文本 | 首段音频到达播放入口 | 完整文本 | 全部音频合成/交付 | 片段数 |
| --- | --- | --- | --- | --- | --- |
| 冷启动 | 3.313 秒 | 6.434 秒 | 4.396 秒 | 11.542 秒 | 7 |
| 已加载模型 | 0.158 秒 | 0.353 秒 | 0.821 秒 | 3.586 秒 | 5 |

两轮均为 44100Hz，短期历史与完整回复一致，运行状态包含“正在流式朗读…”。热启动首段确实早于完整文本；冷启动受语音模型加载影响，首段晚于文本结束，但无需等待整篇语音合成。“全部音频合成/交付”不含真实扬声器把整段读完的时间；对应生成音频长度分别为 15.964 秒和 9.834 秒。

本机证据在忽略目录：`data/verification/streaming-report.json`、`streaming-cold.wav` 和 `streaming-warm.wav`。测试结束已停止所属 llama 进程并释放语音模型。

保留的失败与修正：16:48 首次尝试热启动首段约 0.837 秒，完整文本约 0.81 秒，未达到该样例的早播要求。原因是首段软标点阈值过长；新增短问候提前分段测试，将首段阈值单独降低后得到上表结果。失败记录保留于 `data/verification/streaming-first-attempt.json`，没有删掉或改写为首次即通过。

最终回归（16:53）：`pytest -q` 为 74 passed in 2.48s；Ruff 和 compileall 通过。`python -m pet --smoke` 原生生命周期检查正常退出，日志确认角色加载、热键注册、默认关麦、热键注销及模型释放；该模式关闭自动观察，没有采集工作画面。代码复查核对增量回调的兼容性、队列上限、取消时先停播放与线程收尾、文本/记忆提交时点，以及自动观察仍完整判定后输出。

## 尚需完成与实际边界

剩余本轮工作：最终回归和 lint、应用启动检查、更新 GitHub 及投入记录。用户实际扬声器的听感、不同设备/负载、Kokoro 音色速度仍需结合使用体验，不能用静音接收器代验。

首段延迟随提示长度、冷启动、模型与音色而变化；极短回复可能在首段合成前已经完成文本。流式分段不是原生声学逐采样输出，不是全双工。商业分发的模型、Qt、图片等独立许可边界仍见 [THIRD_PARTY.md](THIRD_PARTY.md)。
