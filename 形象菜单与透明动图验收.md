# 形象菜单与透明动图验收

完成日期：2026-09-14。直接上级：《实施规格.md》的形象菜单与透明动图增量。用户于 9 月 13 日要求在软件中替换形象并补充透明动图，9 月 14 日明确要求继续；本轮只修改本桌宠项目。

## 已交付

- 软件顶部“形象 → 更换桌宠形象”，托盘“形象：更换桌宠形象”进入同一窗口。
- 选择图片、浅/深底色播放预览、应用形象、恢复默认玄司。支持 PNG/JPG/BMP 静态图片及透明 GIF、动态 WebP、APNG。普通带底色图片不自动抠图。
- 导入在后台处理，规范化副本留在 `data/appearances/`，不覆盖原图、不上传；应用时原子保存设置，成功后才切换桌宠和托盘。更换外观不重置人设、不关闭正在进行的对话，不要求更改模型或热键。
- 静态图片保留轻微身体动作；动图按输入帧序和时长循环播放，帧时长限定 20–10000ms，未提供时长或值为 0 时使用 100ms。缓存显示帧与透明点击区域，隐藏桌宠或关闭预览即停止播放。全透明的单帧不会覆盖下层窗口的点击区域。
- 原文件移动/删除后副本仍可用。副本损坏或缺失时回退玄司；重新导入相同素材也可以重新应用并刷新缓存。取消预览或迟到导入结果不改变当前形象，已导入的副本不自动删除。

## 验收证据

- `pytest` 62 项通过，Ruff、compileall、`git diff --check` 通过。本轮增加 16 项测试，包含旧设置兼容、透明像素、本机副本持久化、去重、大小限制、损坏/全透明/缺失输入、路径限制、动图帧序与原始时长、GIF/APNG 处置后无拖影、APNG 独立封面跳过，以及形象变更不取消进行中的会话。
- 动图单测为人工绘制的红/蓝/绿三帧，时长分别 60/120/200ms；逐帧抽查可见像素的 RGBA、透明角落和上一帧位置，而非只读取“支持动画”标志。WebP 基准使用有效的 RIFF 动画帧，避免把本机编码器产生的错误素材当作正确输入。
- 真实 Windows Qt 脚本 `scripts/verify_appearance_menu.py` 验证菜单打开、文件选择、后台导入、预览播放及未应用隔离、原位切换、未保存人设输入保留、80/160/100ms 三帧时长、设置持久化、关闭预览停播、删除源图后的重新加载、丢失副本回退、同图重新导入刷新、完全透明帧穿透、后续帧恢复点击区域、磁盘保存失败保留旧形象、恢复默认持久化、麦克风关闭、Esc 关闭后丢弃迟到结果。18 项检查均为 true，另有总通过标志。证据：`data/verification/appearance-menu-report.json`。
- 已人工查看 `data/verification/appearance-menu.png` 自身窗口截图：顶部说明、浅深背景预览、输入限制、“选择本地图片”“恢复默认玄司”“应用形象”按钮均完整显示。截图中的几何角色为人工测试素材，未替换用户当前玄司。
- `scripts/verify_xuansi.py` 检查默认玄司六档大小/六种身体动作无裁切、点击拖动、透明区域穿透和被动显示不抢焦点；均通过。`scripts/verify_menus.py` 回归配置保存、日志过滤、原生快捷键、右下角位置、收起与退出释放；全部通过，同时确认面板/托盘的新菜单存在。
- 所有验收使用临时设置、人工图片/对话和临时记忆，没有保存用户屏幕、用户原图、真实录音或记忆正文作为测试证据。

## 素材处理依据与限制

查阅 [Pillow Image 文档](https://pillow.readthedocs.io/en/stable/reference/Image.html)、[GIF/APNG/WebP 格式说明](https://pillow.readthedocs.io/en/stable/handbook/image-file-formats.html)、[Qt 文件选择文档](https://doc.qt.io/qtforpython-6/PySide6/QtWidgets/QFileDialog.html) 及本机 Pillow/PySide6 源码。WebP 人工基准依据 [Google WebP RIFF 规范](https://developers.google.com/speed/webp/docs/riff_container)。

本机 Pillow 的动态 WebP 编码最小复现会丢失 alpha；独立静态 WebP 及有效透明动态 WebP 的读取正常。未替换依赖或声称修复上游，本轮采用 APNG 作为动图持久副本，逐帧像素检查已经覆盖三种输入格式。APNG 的合成帧解码用于正确处理 disposal/blend，独立封面不进入动画。

输入最多 20 MB、单帧 1600 万像素，动图最多 120 帧，原始累计解码不超过 1.6 亿像素；静态图长边最多 1536，动图长边最多 512 且副本累计不超过 1200 万像素。超限提示缩小素材，不悄悄丢帧。动图缓存由所有回应状态共享，按原图循环，不新增口型、骨骼或逐状态动画编辑。处理及首次缓存构建有开销，不承诺任意素材都能零延迟导入。

预览导入也会在本机留下去元数据副本，恢复默认不会自动清理旧副本；该行为已写入 README。此前的角色设计许可记录继续适用，用户自行导入素材没有向 GitHub 发布。

## 本机启用与投入

11:10:25 启动新版，启动器 PID 63440、解释器 PID 11648。旧锁文件记录的 PID 63404 已不在运行，本次没有终止其他应用。11:10:32 日志确认桌宠就绪、麦克风默认关闭；随后整屏捕获与本地图文推理正常。运行优先级为 BelowNormal，原配置/长期记忆文件的启用前后 SHA256 一致；只在本地验证记录中保留摘要，未打印正文。证据：`data/verification/appearance-restart.json` 与本机运行日志。

实际投入：约 30 分钟协作经过时间，按 5 分钟取整。依据 9 月 13 日 20:16:12 的命令至 20:21:06 创建动画导入器的时间、9 月 14 日 10:51:04 恢复工作至 11:14 的实现/验收/启动和记录命令估算；可能包含短工具等待，不冒充精确主动工时。跨夜等待全部排除，不计作用户线下人工投入。

本轮本机功能验收为 A：约定菜单、透明动图播放、持久化和恢复均已实现并通过测试及必要的窗口抽查，已启用。没有把这些记录表述为用户已确认自己的所有真实素材；实际素材出现兼容问题时可根据界面错误和日志继续定位。下一动作是打开“形象 → 更换桌宠形象”，选择自己的图片预览并应用。
