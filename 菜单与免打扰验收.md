# 菜单与免打扰增量验收

更新：2026-09-13 15:12 已按用户明确要求重启并启用新版。下方“待重启”为 15:00 的历史状态，重启确认见文末。

2026-09-13；直接上级为《菜单与免打扰增量规格.md》，总任务为《实施规格.md》。仅修改独立桌宠项目及其虚拟环境，没有修改用户设置、长期记忆或父工作区计划。

## 状态

B：四项增量已实现并通过自动检查和原生界面验收，新依赖已安装。旧版用户面板目前仍开着，已询问更新时机以保留可能未保存的内容，尚未替用户重启；下次退出重开使用新版。真实音乐/通话中的主观体验尚待用户确认，不能写作用户已验收。

## 验收证据

| 项目 | 结果与证据 |
| --- | --- |
| 自动检查 | 40 项 pytest 通过；Ruff、compileall、pip check 通过；覆盖旧设置兼容、越界参数、采样请求和引擎启动参数、热键冲突与回滚、日志截尾/轮转、语音开口前阻止及播放中打断 |
| 日志菜单 | 软件及托盘菜单可用；异步读取、WARNING 筛选、隐藏后停止计时器通过；`data/verification/menu-report.json` 和 `menu-logs.png` |
| 配置菜单 | 虚拟配置 temperature=0.4 / max_tokens=256 保存到测试专用文件，并传至回复任务；参数页截图已人工检查，内容较多时纵向滚动；`menu-configuration.png` |
| 快捷对话 | 真实 RegisterHotKey 注册、WM_HOTKEY 事件经 Qt 原生过滤器打开窗口；输入焦点、回车发送、双窗口同步回复、Esc 收起、退出注销通过。窗口位于工作区右下角，右边/下边距均 16px；`menu-report.json`、`menu-quick-chat.png` |
| 默认组合 | 单独注册并注销 Ctrl+Alt+Space 成功，当前机器未发现冲突；界面测试使用 Ctrl+Alt+Shift+F24 避免影响常用组合 |
| 实机声音会话 | 在本机建立新的子进程零样本输出，以及本进程零样本输出，均被系统会话通知捕获；没有录音或发出测试提示音，没有改其他软件音量；`audio-session-report.json` |
| 采样开销 | 原型每 100ms 重建 COM 图约占单核 4.67%；改为通知与缓存后，同机 5.02 秒样本约 0.62%，采样全部有效、最大采样间隔 112.24ms，工作线程正常退出；`audio-activity-report.json` |
| 隐私 | 界面检查仅使用虚拟对话、虚拟日志和测试专用设置/记忆文件；截图只含软件自身窗口；新增日志仅状态、错误类型，无会话或音频正文 |

非零电平的自身排除、外部静音/音量零排除、声音间隔保持 1.5 秒、采样失效保持文字、合成期间开始声音和播放中打断，使用假电平/假声卡自动验证。真实会话通知使用全零输出验证，不能冒充真实音乐的听觉验收。

Windows 电平采样约 100ms，播放状态检查约 50ms，新增会话通过通知加入，设备列表每秒刷新；无法保证突然发声时零重叠。极短提示音、部分独占输出和设备切换仍需实际检查。被跳过的回复不补播；界面和气泡已经先显示文字。

## 查证与实现依据

- [Microsoft：RegisterHotKey](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-registerhotkey) 与 [Qt 原生事件过滤器](https://doc.qt.io/qtforpython-6/PySide6/QtCore/QAbstractNativeEventFilter.html)：不使用键盘记录钩子。
- [Microsoft：音频会话枚举](https://learn.microsoft.com/en-us/windows/win32/api/audiopolicy/nf-audiopolicy-iaudiosessionmanager2-getsessionenumerator)、[电平接口与独占模式限制](https://learn.microsoft.com/en-us/windows/win32/coreaudio/peak-meters)，以及 [pycaw 源码](https://github.com/AndreMiras/pycaw)：MTA 线程中保留新会话通知，避免只依赖旧枚举器。
- [llama.cpp server 参数](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/README.md)：上下文和 CPU 参数进入子进程启动，采样参数逐请求发送。

## 实际投入与剩余项

本轮开始时间为 14:30:41（Asia/Shanghai）；截至 15:00，约 30 分钟主动投入估算，按 5 分钟取整，依据本轮连续命令和验收时间戳。短命令等待包含在估算中，未计之前用户自用或空闲时段；不是用户线下工时。等待用户选择生效时机期间继续完成文档与代码检查，没有计入无人工作的等待。

剩余：按用户选择启用新版；体验音乐/视频播放时的自动文字回应和连续语音听感。下一动作是退出旧版后从原快捷方式启动，再按 Ctrl+Alt+Space 打开快捷对话，非重新安装或下载模型。

## 15:12 重启确认

用户明确要求“重启”。已通过旧实例的 Qt 应用退出消息结束旧版，并确认旧解释器、启动器及其模型进程均已退出；15:11:50 从原虚拟环境启动新版，没有强制终止其他软件，也未写入或删除用户设置和记忆文件。

`data/app.lock` 对应新版解释器 PID 16704，启动器 PID 35684，只有一个桌宠应用实例。15:11:51 日志确认快捷键注册成功、鼠标监听启动、麦克风默认关闭；声音检测已识别其他软件输出并进入“仅文字”。15:11:56 本地图文引擎就绪并完成一次观察回应。启用新版这项已经完成；音乐/通话使用感受仍需用户体验，不写成用户已验收。

本次重启协作时间为 15:08:38–15:12，约 5 分钟主动投入估算，按 5 分钟取整，依据终端和启动日志；与上轮之间的空闲时间未计入。下一动作可直接按 Ctrl+Alt+Space 使用快捷对话。
