# 本地 AI 桌宠实施规格

生效时间：2026-09-12 21:16（北京时间）。直接上级：本次用户请求及已审核方案。

2026-09-13 更新：增加已获用户确认的长期记忆；默认朗读模型在实测后改为 Melo 中英，Kokoro 保留为可选。变更只涉及本桌宠的语音和记忆，不影响原工作区的 Android 或接单计划。验收证据及未完成的真实设备体验项见 `验收记录.md`。

用户已经授权实施、增加语音、替换模型。语音为手动开启连续对话，自动断句，随时关闭。按这一授权直接分解和执行，不重复索取实施许可。

## 交付能力

1. 本机看图与聊天：优先验证 Qwen3.5-4B Q4_K_M + 匹配视觉投影，通过 llama.cpp CUDA 12.4 运行；这是功能替代，不能宣称所有任务质量与 MiniCPM 完全相同。
2. 本机语音：SenseVoice int8 识别中文，Melo 中英合成语音，Kokoro int8 为可选。连续对话按钮开启麦克风，静音结束一轮，播报阶段暂停识别避免声学回声；按钮可停止播放和收音。应用启动时不自动开麦。
3. 桌宠复用 NekoAI 的猫咪精灵素材、动画定义和许可，使用 Python/PySide6 原生 Windows 窗口适配本地引擎，避免为本机安装完整 Rust/C++ 构建工具链。上游源码保留在 `upstream-nekoai` 供追溯；不宣称这是原版 NekoAI 二进制。
4. 可编辑名字、人设、系统提示词、观察间隔、宠物大小、语音音色和语速。保存后生效并在重启后保留；不持久化屏幕、麦克风录音或聊天正文。
5. 鼠标停留、点击、拖拽结束后读取鼠标所在显示器的完整画面，长边最多 1600 像素，可切回周围裁剪；单并发、限频和过期取消。文字/动作回应；自动观察默认不朗读，手动及语音对话按开关朗读。
6. 桌宠与自动气泡不抢焦点，透明区域穿透；只有用户打开控制面板才取得输入焦点。全屏及资源不足暂停自动观察，一键休眠停止模型释放资源。
7. 长期记忆只存用户明确要求记住的事项或手动编辑的文本；可编辑、清空和重启恢复。短期会话仅在内存，清空记忆会取消当前任务并清掉短期上下文。

## 结构及约束

`pet/` 放应用模块，`tests/` 放有意义的逻辑和生命周期测试，`scripts/` 放下载及验收脚本，`assets/neko/` 放上游精灵和定义，`runtime/` 放已校验推理引擎，模型放 `D:\AI\Models\desktop-pet`。

Python 3.13，异步 HTTP 与子进程；推理、语音和文件操作离开 UI 线程。回调只入队；所有 Qt 窗口操作在主线程。模块超过约 300 行且职责可拆分时拆分。关键路径有中文注释和无敏感正文的日志。

服务只监听 `127.0.0.1`，每次启动随机会话凭据，不写到文件。运行期 HTTP 客户端禁用环境代理。屏幕文字作为不可信内容，模型输出仅显示/朗读/映射到预设动作，绝不执行系统命令。下载仅取公开仓库，权重及发布包验证 SHA256。

## 执行顺序

- [x] 获取并校验运行环境和本地权重；先跑真实图文与语音样例。
- [x] 实现带资源释放、取消和短上下文的本地推理服务。
- [x] 实现连续语音识别、自动断句与中文播报。
- [x] 实现桌宠、可编辑人设、聊天、长期记忆和托盘控制。
- [x] 接入鼠标局部观察与不打扰策略。
- [x] 运行测试、虚拟场景检查、资源测量；制作启动入口和使用说明。
- [x] 从实际运行日志核实连续收音、回复及休眠释放。
- [ ] 用户对识别准确性、听感及日常工作流畅度的确认；不能用自动测试或运行日志替代主观体验。

## 验收

用虚拟窗口和人工基准验证图文匹配，不采集用户真实工作作为测试证据。用已知中文测试语音验证识别，用生成音频核验采样率、内容可识别性及真实播报路径。自动化不能替代用户真实麦克风和主观流畅度体验，未验证项需在交付记录中明确列出。

验证暂停中止推理和音频、关闭后无遗留模型进程；迟到结果不能覆盖新设置或恢复已关闭语音。记录图文响应时间、语音耗时、待机和推理资源占用。通过后提供本机可双击运行入口。

## 2026-09-13 菜单与免打扰增量

用户追加软件内日志、模型参数菜单、其他应用发声时回避、右下角快捷对话。当前实施增量见 [菜单与免打扰增量规格.md](菜单与免打扰增量规格.md)，结果见 [菜单与免打扰验收.md](菜单与免打扰验收.md)。原有本地处理、记忆和手动启麦约定不变；本轮没有更换模型或改成流式语音。

## 2026-09-13 整屏观察调整

用户补充“它能看到的是整个屏幕”并要求继续，交付能力第 5 项由默认鼠标附近裁剪调整为默认单显示器整屏，偏好中保留局部选项。影响截图范围、附图入口文案和对应验收脚本，不改变本机处理、限频和全屏游戏暂停策略。依据、验证和投入见 [整屏观察验收.md](整屏观察验收.md)；初版局部观察的历史证据保留。
