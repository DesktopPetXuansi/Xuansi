# 2026-09-14 声音回避菜单开关验收

直接上级：[实施规格](实施规格.md) 的“声音回避菜单开关”增量，依据用户“声音回避需要可以关闭和开启，在软件菜单中”的请求。当前普通文字/语音回复仍使用已交付的分段流式朗读；自动观察仍先完整判断是否回应。

## 当前状态与投入

- 可验证开始：2026-09-14 17:16:14（北京时间，首次工具及源码核对）。
- 当前检查：17:27，功能、回归、原生菜单及本机重启检查通过；当前 B 级，剩余 GitHub 提交同步。
- 截至当前约 10 分钟主动投入估算，依据 17:16–17:27 的连续工具及修改记录，按 5 分钟取整，可能包含短暂命令等待；不计入用户线下时间。

## 实现与验收证据

- `audio_avoidance` 为布尔设置，默认开启；旧设置缺少字段时仍开启，关闭后可保存并重载。类型错误按既有配置校验拒绝。
- 主面板“配置”菜单和托盘使用同一个可勾选 QAction，显示“声音回避（其他软件发声时静音）”。采用用户 `triggered` 事件保存，程序恢复勾选不触发二次写入。依据 [Qt QAction 官方文档](https://doc.qt.io/qtforpython-6/PySide6/QtGui/QAction.html) 及现有设置源码核对。
- 开关异步保存，成功后在线改变声音策略，失败恢复旧勾选、旧配置和旧运行策略。只改回避不检查图文模型文件、不重配热键、不关闭麦克风、不取消文字生成、不覆盖人设和采样参数表单草稿。
- 关闭后放行音频并停止设备电平查询、释放查询对象；重新开启后等待新检测结果。新策略共用于流式回复、观察朗读和试听，仍保留取消/休眠规则；已被回避丢弃的整轮不会补播。

17:23 回归：`pytest -q` 为 **78 passed in 2.63s**；Ruff、compileall、`git diff --check` 通过。新增测试覆盖旧设置默认值、关闭值持久化、无效类型、回避关闭时放行、重开等待新样本、只切换策略不中断，以及播放中重开立即停止、再次关闭不补播。

原生验证命令：`.venv/Scripts/python.exe -X utf8 scripts/verify_audio_avoidance.py`。使用临时设置、临时记忆、虚拟未完成对话和不存在的模型路径；不启麦、不播放、不读取工作屏幕。13 项检查均通过：共享菜单、默认勾选、无模型时保存关闭、菜单同步、放行状态、两窗口提示同步、对话保留、草稿保留、重载状态、模拟磁盘失败回退、重新开启并保存、仍为同一对话、完整文字最终显示。

结果在本机忽略目录 `data/verification/avoidance-menu-report.json`；原生菜单截图 `avoidance-menu.png` 已人工查看，文字和勾选清楚，无裁切。测试输出中的 OSError 是主动构造的保存失败用例，不是用户设置写入失败。

本机新版于 17:26:14 启动，日志确认形象、热键及默认关麦状态正常。重启前后的长期记忆 SHA256 相同。设置哈希检查发现变化后停止覆盖并核对只读日志：17:26:34 出现“设置已保存”和“声音回避开关 enabled=False”，随后开始了真实语音交互。该变化与菜单保存关闭策略一致，因此保留当前设置，不把它误判为重启损坏而恢复旧文件；不读取或发布用户语音/聊天正文，不把运行日志当成主观听感确认。

## 剩余与边界

剩余本轮动作：提交同步并核对远端。真实电平采样仍约 100ms，不能承诺突然出现的声音零延迟回避；关闭后的同时播放是用户可选行为。功能开关不改变模型和图片的许可范围。
