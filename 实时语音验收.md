# 2026-09-20 说完后尽快回复验收

直接上级：[实施规格](实施规格.md) 和 [实时对话规格](实时对话规格.md) 中 2026-09-20 的生效范围。
用户澄清为“我话说完了之后，模型可以立刻回话”，因此取消未发布的提前应答原型，交付在线识别、
开麦预热、结束后才回复和既有流式朗读。不承诺零延迟。

## 功能与验证

- 说话时显示临时转写，主面板和快捷对话框均为纯文本。中间文字不提交模型、不写入长期记忆。
- 每 100ms 输入一块音频，约 0.4 秒低能量停顿判定结束；补模型前瞻静音取得尾字后只提交一次。
  持续说话超过 15 秒也不会因为时长而抢答。噪声、很轻的声音及句中较长停顿仍可能影响断句。
- 开启连续对话先加载本地图文引擎并在内存预热 TTS，再开启实时收音。首次准备仍需要数秒。
- 朗读期间暂停收音，结束后恢复；关闭、休眠或新的请求取消旧任务。准备过程中关闭不会迟到开麦，
  若新文字请求打断预热，则退出启麦状态并提示重新开启。
- “配置 → 语音与行为”新增默认开启的“实时识别（说完后尽快回复）”，保存关闭后使用旧 SenseVoice
  整句识别。新增权重下载已校验，目录固定规则沿用原项目，不新增 Python 运行依赖。

2026-09-20 16:51 最终回归：**86 passed in 2.82s**；Ruff、compileall 通过。
测试覆盖句中不答、短停顿不提交、400ms 提交、长句不抢答、重复/旧设备结果丢弃、临时识别不写记忆、
朗读静音收音及恢复、预热期间关闭和文字请求中断。原有回避、流式、记忆、形象和菜单测试继续通过。

真实模型检查：`scripts/verify_live_conversation.py` 使用合成固定短句、模拟声卡输入、静音播放接收器、
临时记忆及真实本地 Paraformer / Qwen / Melo；不读取真实麦克风或工作屏幕。
16:47 的最终方案结果如下：

| 指标 | 实测 |
| --- | --- |
| 合成输入实际语音长度 | 2.266 秒 |
| 开始说话到首个临时文字 | 0.663 秒 |
| 说完到结束判断及最终文字 | 0.385 秒 |
| 说完到首段音频进入播放接口 | 1.122 秒 |
| 说话期间提前朗读 | 没有 |
| 朗读暂停收音、结束恢复、关闭释放设备 | 通过 |

转写出现了一个局部重复字，未将本次结果写成字字准确。延迟为已预热的短句样例，不包括真实声卡缓冲，
也不是每种问题/模型/音色的保证；长首句、附图请求和冷启动可能更慢。

`scripts/verify_realtime_ui.py` 的 8 项原生控件检查通过：菜单定位、默认开关、临时文字不发请求、
两窗口纯文本预览、最终提交、聊天同步、旧设备事件拒绝、保存重载。只捕获应用自有控件，
`data/verification/realtime-preferences.png` 已查看，选项清楚可见；截图中的关闭状态是临时设置用例。

本机于 16:51 启动，日志确认角色、热键和默认关麦正常；新字段有效值为 true，声音回避仍为用户的 false。
真实设置与记忆文件没有由验收脚本覆盖。启动前 `memory.json` 已不存在，没有读取或创建记忆内容。
本机报告均位于忽略目录 `data/verification/`，未上传模型、运行数据、真实语音、设置或屏幕内容。

## 进度与投入

9 月 15 日的原型准备约 20 分钟主动投入估算（18:04:58–18:21 的工具记录）；该原型未交付，随后按用户
澄清删除提前应答、双向声卡与回声消除代码。保留这一变更经过，不把原型测试当作最终方案证据。

本轮从 2026-09-20 16:38:39 起；截至 16:52，实际投入约 15 分钟主动投入估算，按 5 分钟取整，
依据连续命令与验证时间，可能含短暂工具等待；不计跨日中断和用户线下时间。
16:54 补记：功能、本机启动、文档及 GitHub 发布核验均完成，当前 A 级。功能提交
`ac1f8a761b6f7373e5790618a74835d615861a5d` 已快进合入 main 并正常推送，GitHub API 返回相同 SHA。
22 个发布文件、42 个本地文档链接检查通过，检查模式未发现秘密或运行数据；仓库既有 LICENSE
blob 保持不变。启动前后设置 SHA256 相同，长期记忆文件启动前后均不存在，未创建或覆盖。

本轮最终实际投入约 15 分钟主动投入估算（16:38:39–16:54），估算规则同上；加上 9 月 15 日原型阶段
约 20 分钟，累计约 35 分钟。无尚未完成的约定开发或发布项；实际房间中的听感仍未由用户确认。
下一步是在软件中手动开启连续对话，等待“实时聆听中”后试用；不能把自动验证记为用户已验收。
